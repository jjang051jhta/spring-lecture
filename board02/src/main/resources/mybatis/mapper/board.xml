<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jjang051.board02.dao.BoardDao">
    <insert id="writeBoard" parameterType="BoardDto">
        insert into board(id,title,content,writer,regdate,hit,password) values
        (board_seq.nextval,#{title},#{content},#{writer},#{regdate},1,#{password})
    </insert>
    <select id="findAll" resultType="BoardDto">
        select * from board
    </select>
</mapper>